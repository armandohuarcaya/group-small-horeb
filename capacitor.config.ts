import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'group-small-horeb',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
